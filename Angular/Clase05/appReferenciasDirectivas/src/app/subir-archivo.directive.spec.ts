import { SubirArchivoDirective } from './subir-archivo.directive';

describe('SubirArchivoDirective', () => {
  it('should create an instance', () => {
    const directive = new SubirArchivoDirective();
    expect(directive).toBeTruthy();
  });
});
